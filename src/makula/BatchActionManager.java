/*package makula;


import java.util.ArrayList;
import java.util.List;

public class BatchActionManager extends BatchEditor {

    List<MakImage> prevState = new ArrayList<MakImage>();
    List<MakImage> state = new ArrayList<MakImage>();

    public BatchActionManager(MakImage image) {
        setState(image);
        setPrevState(null);
    }

    public MakImage getState() {
        return state.get(1);
    }

    public void setState(List<MakImage> newState) {
        state = newState;
    }

    public MakImage getPrevState() {
        return prevState;
    }

    public void setPrevState(MakImage newPrevState) {
        prevState = newPrevState;
    }

}*/